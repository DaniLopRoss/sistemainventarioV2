<h1>Departaments</h1>

<div class="col-lg-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                 
                  <p class="card-description">
                    
                  </p>
                  <div class="table-responsive pt-3">

                  <table class="table table-bordered" id="tabladepartamentos">
                  <thead class="thead-blue" >
                    <tr>
                      <th scope="col">ID</th>
                      <th>NOMBRE</th>
                      <th>Num_equipo</th>
                      <th>Area</th>
                      <th>Acciones</th>
                     
                      
                    </tr>
                  </thead>
                  
                  <% @departaments.each do |departamento| %>
                   
                    <tr>
                        <td><%= departamento.id %></td>
                        <td><%= departamento.nombre %></td>
                        <td><%= departamento.num_equipos %></td>
                        <td><%= departamento.room.nombre   %></td>
                      <td > 
                           <a href="<%= edit_departament_path(@departaments)%>" class="edit"><i class="bi bi-pencil-square"></i></a>
                
                      <a  class="mostrar"  <%= link_to ({:action => :show, :id => departamento.id}) do %><i class="bi bi-eye-fill"></i><%end%></a> 
                     
                       <a class="eliminar" <%= link_to ({ :action => :destroy, :id => departamento.id })do%>
                      <i class="bi bi-trash"></i>
                    
                      <% end %>
                      </a>

                      </td>
                      
                      </tr>
                </div>
                
                
                <% end %>
                    <a href="<%= new_departament_path %>" class="new"><i class="bi bi-plus-circle-fill"></i></a>
                  
                    
                    </div>
                </div>
              </div>
            </div>


<script>
            $(document).ready(function() {
              var oTable = $("#tabladepartamentos").DataTable({
                language: {
                  url: '//cdn.datatables.net/plug-ins/1.13.1/i18n/es-MX.json'
              }
              });
      } );
            </script>