<head>
<%= stylesheet_link_tag "style" %>
</head>
<div align="center">
<p style="color: green"><%= notice %></p>
<ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="">Areas</a></li>
          <li class="breadcrumb-item active">Lista areas</li>
        </ol>
<h4>Àreas</h4>

<div class="col-lg-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                 
                  <p class="card-description">
                    
                  </p>
                  <div class="table-responsive pt-3">

                  <table class="table table-bordered" id="tableareas">
                  <thead class="thead-blue" >
                    <tr>
                      <th scope="col">ID</th>
                      <th scope="col">NOMBRE</th>
                      <th scope="col">Departamento</th>
                      <th scope="col">ACCIONES</th>
                     
                      
                    </tr>
                  </thead>
                  
                  <% @areas.each do |area| %>
                   
                    <tr>
                        <td><%= area.id %></td>
                        <td><%= area.nombre %></td>
                        <td> <%= area.departamento%> </td>
                      <td > 
                      
                      <a href="<%= edit_area_path(area)%>" class="edit"><i class="bi bi-pencil-square"></i></a>
                      <a href="<%= area_path(area)%>" class="mostrar"><i class="bi bi-eye-fill"></i></a>  
                   <a class="eliminar" <%= link_to ({ :action => :destroy, :id => area.id })do%>
                  <i class="bi bi-trash"></i>
                  <% end %>
                  </a>
                  </td>
                      </tr>
                </div>
                
                
                <% end %>
                    <a href="areas/new" class="new"><i class="bi bi-plus-circle-fill"></i></a>
                  </div>
                </div>
              </div>
            </div>

<script>
            $(document).ready(function() {
              var oTable = $("#tableareas").DataTable({
                language: {
                  url: '//cdn.datatables.net/plug-ins/1.13.1/i18n/es-MX.json'
              }
              });
      } );
            </script>















