<%= form_with(model: reportesistema) do |form| %>
  <% if reportesistema.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reportesistema.errors.count, "error") %> prohibited this reportesistema from being saved:</h2>

      <ul>
        <% reportesistema.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
