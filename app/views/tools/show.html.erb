<head>
<%= stylesheet_link_tag "style" %>
</head>
<div class="container">
<div class="card mt-5">
<div class="card-header">
<ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="">Herramienta</a></li>
          <li class="breadcrumb-item active">Características de herramienta</li>
        </ol>



<h5 class="text">Características de herramienta <%= @tool.id %> </h5>
</div>
 <div class="probar">


<%= render @tool %>
<br>
</br>
<div>
<%if can? :edit, Tool%>
  <%= link_to "Editar", edit_tool_path(@tool),   class: "btn btn-warning btn-xs round btn-editar "%>
  <%end%>  
    <%if can? :destroy, Tool%>
  <%= link_to "Eliminar", @tool, method: :delete, data: { confirm: "Esta seguro de eliminar"},    class: "btn btn-danger btn-eliminar" %>
  <%end%>
  <%= link_to "Regresar", tools_path ,  class: "btn btn-success btn-xs round btn-regresar " %>
  <br>
  </br>
</div>
