<%= form_with(model: herramientum) do |form| %>
  <% if herramientum.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(herramientum.errors.count, "error") %> prohibited this herramientum from being saved:</h2>

      <ul>
        <% herramientum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.label :caracteristicas, style: "display: block" %>
    <%= form.text_area :caracteristicas %>
  </div>

  <div>
    <%= form.label :existencia, style: "display: block" %>
    <%= form.number_field :existencia %>
  </div>

  <div>
    <%= form.label :estatus, style: "display: block" %>
    <%= form.select :estatus, ["Guardado", "Activo", "Inactivo"], prompt: "Seleccione el estatus" %>
  </div>

  <div>
  <button class="btn btn-info" <%= form.submit %> Guardar </button>
  </div>
<% end %>
